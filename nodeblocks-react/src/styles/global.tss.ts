import spacing from '@material-ui/core/styles/spacing';

/**
 * Global style constants
 */

export const flex = {
  display: 'flex'
}

export const padding = {
  s1: spacing.unit,
  s2: spacing.unit * 2,
  s3: spacing.unit * 4,
}